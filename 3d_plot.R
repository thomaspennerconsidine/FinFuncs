{
  #TRADING_X <- (TRD_DATA) #1
  # L <- list()
  # L$MARKET_1 <- xts( MARKET_1, as.Date((first(n)):(last(n))) )
  # L$TRADING_X <- xts( TRADING_X, as.Date((first(n)):(last(n))) )
  #
  # OUTPUT_1 <- do.call("merge", L)
  #________________ BASE
  # MKT <- (OUTPUT_1$MARKET_1)
  #MKT$Position_1 <- (OUTPUT_1$Lag.1) 
  # MKT$Price_1 <- (OUTPUT_1$MARKET_1)
  # XX <- MKT$Price_1
  # XX$back_A <- MKT$Position_1
  # PP <- dailyReturn(XX$Price_1)
  # POSS <-(lag.xts((XX$back_A), k = 1)* dailyReturn(XX$Price_1) )
  # X_VAL <- T_X
  # Y_VAL <- T_A40
  #REZ <- 200
  # x_c <- cut(X_VAL, REZ)
  # y_c <- cut(Y_VAL, REZ)
  # z <- table(x_c, y_c)
  # image2D(z=z)
  # hist3D(z=z)
} # Simulate from a Multivariate Normal Distribution (NOT ACTIVE)